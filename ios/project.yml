name: test-evowit
options:
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: 5.0
    MARKETING_VERSION: 1.0.0
    CURRENT_PROJECT_VERSION: 3
    DEVELOPMENT_TEAM: 76PHSCHPCK
    CODE_SIGN_STYLE: Automatic
  configs:
    Debug:
      CODE_SIGN_STYLE: Automatic
    Release:
      CODE_SIGN_STYLE: Manual
      CODE_SIGN_IDENTITY: Apple Distribution
      PROVISIONING_PROFILE_SPECIFIER: test-evowit App Store
      PROVISIONING_PROFILE: bbce9b98-170f-45b6-81e9-a488d946fe84
packages: {}
schemes:
  TestEvowit:
    build:
      targets:
        TestEvowit: all
targets:
  TestEvowit:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - TestEvowit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.we555.test-evowit
        INFOPLIST_FILE: TestEvowit/Info.plist
        PRODUCT_NAME: test-evowit
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    info:
      path: TestEvowit/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleDisplayName: test-evowit
        UILaunchScreen: {}
        NSCameraUsageDescription: Allow camera access to capture homework questions.
        NSPhotoLibraryUsageDescription: Allow photo access to import homework images.
        NSPhotoLibraryAddUsageDescription: Allow photo library access to save solved screenshots.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        BACKEND_BASE_URL: http://120.197.118.22:21080
